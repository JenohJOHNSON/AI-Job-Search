import { NextResponse } from "next/server";
import { requireUser, routeError } from "@/lib/api";
import { db } from "@/lib/db";
export async function GET() { try { const user = await requireUser(); const data = await db.user.findUniqueOrThrow({ where: { id: user.id }, select: { id: true, email: true, createdAt: true, updatedAt: true, profile: true, resumes: { select: { id: true, fileName: true, mimeType: true, size: true, contentHash: true, sourceText: true, extractionStatus: true, suggestions: true, createdAt: true } }, searchProfiles: { include: { queries: true } }, applications: { include: { history: true, job: { include: { sources: true, analyses: true } } } }, documents: true } }); return new NextResponse(JSON.stringify({ exportedAt: new Date().toISOString(), data }, null, 2), { headers: { "Content-Type": "application/json", "Content-Disposition": "attachment; filename=elan-personal-data.json", "Cache-Control": "no-store" } }); } catch (error) { return routeError(error); } }
